use azure_functions::{
    bindings::TimerInfo,
    func,
};

#[func]
pub fn {{name}}(
    #[binding(schedule = "{{schedule}}")] trigger: TimerInfo
) {
    // function will execute at the scheduled interval
}
